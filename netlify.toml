# netlify.toml (en la raíz del repo)
[build]
  base    = "frontend"
  command = "npm ci && npm run build"
  publish = "frontend/.next"

[build.environment]
  NODE_VERSION = "20.16.0"  # o 20.x estable
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
