[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"            # Para Next.js en Netlify con plugin

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
