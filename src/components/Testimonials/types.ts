export interface TestimonialData {
  author: string;
  avatarUrl?: string;
  country: string;
  countryCode?: string;
  quote: string;
  reviewUrl?: string;
}

export interface TestimonialsContent {
  eyebrow?: string;
  subtitle?: string;
  title?: string;
  viewFullReviewLabel?: string;
}
